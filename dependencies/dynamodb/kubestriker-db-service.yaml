apiVersion: v1
kind: Service
metadata:
  name: db-service
  labels:
    name: kubestriker
    app: kubestriker
spec:
  type: ClusterIP
  selector:
    app: kubestriker
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8000
      name: db-service